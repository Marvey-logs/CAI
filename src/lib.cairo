
pub mod contracts {
    pub mod schoollibrary;
    pub mod studentrecord;
}


pub mod interfaces {
    pub mod ischoollibrary;
    pub mod istudentrecord;
}
// use interfaces::*;   //this star function imports everything from the interface.
//use interfaces::Ischoollibrary; // this imports the ischoollibrary alone function from the interface module. 
// use interfaces::{Ischoollibrary, name::gida}; this is used to selectively choose the interfaces to import


// use contract::{SchoolLibrary, StudentRecord};

// use core::starknet::ContractAddress;


// use crate::interfaces::

